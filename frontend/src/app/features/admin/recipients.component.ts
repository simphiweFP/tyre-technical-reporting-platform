import { Component, inject, signal } from '@angular/core';import { FormsModule } from '@angular/forms';import { ReportDeliveryService } from '../../core/delivery/report-delivery.service';import { ReportRecipient } from '../../shared/models/report.models';
@Component({selector:'app-recipients',imports:[FormsModule],templateUrl:'./recipients.component.html',styleUrl:'./admin.component.scss'})
export class RecipientsComponent{private readonly delivery=inject(ReportDeliveryService);readonly showForm=signal(false);readonly query=signal('');readonly recipients=signal<ReportRecipient[]>([]);readonly editing=signal<ReportRecipient|null>(null);readonly notice=signal('');newRecipient={company:'',contact:'',email:'',cc:'',branch_code:'All Branches',category:'All Categories',escalation_hours:24};constructor(){void this.load()}filtered(){const q=this.query().toLowerCase();return this.recipients().filter(r=>!q||`${r.company} ${r.email} ${r.branch_code} ${r.category}`.toLowerCase().includes(q))}open(item?:ReportRecipient){this.editing.set(item??null);this.newRecipient=item?{company:item.company,contact:item.contact_name,email:item.email,cc:item.default_cc,branch_code:item.branch_code,category:item.category,escalation_hours:item.escalation_hours}:{company:'',contact:'',email:'',cc:'',branch_code:'All Branches',category:'All Categories',escalation_hours:24};this.showForm.set(true)}async save(){const x=this.newRecipient;const input={company:x.company,contact_name:x.contact,email:x.email,default_cc:x.cc||null,branch_code:x.branch_code,category:x.category,escalation_hours:x.escalation_hours};const current=this.editing();const saved=current?await this.delivery.updateRecipient(current.id,{...input,is_active:current.is_active}):await this.delivery.createRecipient(input);this.recipients.update(items=>current?items.map(i=>i.id===saved.id?saved:i):[...items,saved]);this.showForm.set(false)}async toggle(item:ReportRecipient){const r=await this.delivery.setRecipientStatus(item.id,!item.is_active);this.recipients.update(x=>x.map(i=>i.id===r.id?r:i))}async test(item:ReportRecipient){this.notice.set((await this.delivery.testRecipient(item.id)).message)}private async load(){this.recipients.set(await this.delivery.recipients(false))}}
